# Archivo de ejemplo de variables de entorno para Milos-Shop
# Copia este archivo como .env para desarrollo local
#
# ⚠️  PARA PRODUCCIÓN: Usa Railway dashboard para configurar variables
# Ejecuta: node setup-env.js para generar valores seguros

# Configuración de base de datos PostgreSQL
# Para desarrollo local: usar PostgreSQL local o servicio como Neon
# Para producción: usar la URL proporcionada por el proveedor (Railway, Neon, etc.)
DATABASE_URL="postgresql://username:password@localhost:5432/milos_shop"

# Configuración de sesiones (GENERA UNO SEGURO PARA PRODUCCIÓN)
# Ejecuta: node setup-env.js para generar un valor seguro
SESSION_SECRET="desarrollo-session-secret-cambiar-en-produccion"

# Configuración de WebSockets para admin (GENERA UNO SEGURO PARA PRODUCCIÓN)
# Ejecuta: node setup-env.js para generar un valor seguro
ADMIN_WS_TOKEN="desarrollo-admin-token-cambiar-en-produccion"

# Código secreto para registro de administradores (OBLIGATORIO en producción)
# Evita que cualquiera se registre como admin desde la URL pública.
# Genera un valor seguro con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SIGNUP_SECRET="desarrollo-admin-signup-secret-cambiar-en-produccion"

# Configuración de servidor
NODE_ENV="production"
PORT=5000

# Configuración de CORS (configura para tu dominio en producción)
CORS_ORIGIN="https://tu-dominio.com"

# Configuración de email (opcional - para notificaciones)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="tu-email@gmail.com"
SMTP_PASS="tu-contraseña-app"

# Configuración de pagos (opcional - para procesar pagos)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PUBLISHABLE_KEY="pk_test_..."
MERCADOPAGO_ACCESS_TOKEN="..."

# Configuración de logging
LOG_LEVEL="info"

# Configuración de Sentry para monitoreo de errores
SENTRY_DSN="https://your-sentry-dsn-here"